{
  "name": "lifeos",
  "version": "1.0.0",
  "private": true,
  "description": "LifeOS — Monorepo (Node.js backend + Static frontend)",
  "scripts": {
    "install:backend": "cd backend && npm install",
    "migrate": "cd backend && npm run migrate",
    "dev:backend": "cd backend && npm run dev",
    "start": "cd backend && npm start"
  }
}
